apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Fixed uid so the provisioned dashboard can reference it by name rather
    # than a generated one.
    uid: prometheus
    access: proxy
    # Compose service name, resolved on the internal network.
    url: http://prometheus:9090
    isDefault: true
    editable: false
